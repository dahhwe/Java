/**
 * интерфейс, содержащий одну функцию для анализа строки.
 */
public interface LettersCounter {

    /**
     * Интерфейс, содержащий одну функцию. Данный интерфейс производит анализ строки по интересующему критерию.
     *
     * @param str Строка для проверки.
     * @return Результат проверки.
     */
    int analyse(String str);
}
